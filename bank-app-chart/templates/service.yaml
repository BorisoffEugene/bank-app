apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-app-chart.fullname" . }}
  labels:
    {{- include "bank-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-app-chart.selectorLabels" . | nindent 4 }}
