server:
  port: 8084

spring:
  application:
    name: bank-transfer
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/bank-realm
      client:
        registration:
          bank-transfer:
            provider: keycloak
            client-id: bank-transfer
            client-secret: JnZCXEp1UNHcT3I4TqguwMtfpKt8NB0Q
            authorization-grant-type: client_credentials
        provider:
          keycloak:
            issuer-uri: http://localhost:8080/realms/bank-realm
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

bank:
  notifications:
    base-url: http://localhost:8085
  accounts:
    base-url: http://localhost:8082

logging:
  level:
    root: INFO
    ru.yandex.practicum.transfer: DEBUG
    org.springframework.security: DEBUG
    org:
      hibernate:
        SQL: DEBUG